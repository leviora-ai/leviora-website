apiVersion: v1
kind: Service
metadata:
  name: leviora-website-api
  namespace: leviora-website
  labels:
    app.kubernetes.io/name: leviora-website-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: leviora-website-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
